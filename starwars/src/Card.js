import React from 'react';




const Card = (data) => {
    console.log(props.data);
return (
    <div>card
        <div>
            {props.data.map( p => 
            <App key={data.name}> />
        )
        </div>
    </div>

)
};

export default Card;
